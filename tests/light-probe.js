const port=Number(process.argv[2]||9333),wait=ms=>new Promise(resolve=>setTimeout(resolve,ms));
async function main(){const targets=await fetch(`http://127.0.0.1:${port}/json`).then(response=>response.json()),page=targets.find(item=>item.type==="page"&&!/mini\.html/.test(item.url));const socket=new WebSocket(page.webSocketDebuggerUrl);await new Promise((resolve,reject)=>{socket.onopen=resolve;socket.onerror=reject;});let id=0;const pending=new Map();socket.onmessage=event=>{const message=JSON.parse(event.data);if(!pending.has(message.id))return;pending.get(message.id)(message.result);pending.delete(message.id);};const send=(method,params={})=>new Promise(resolve=>{const call=++id;pending.set(call,resolve);socket.send(JSON.stringify({id:call,method,params}));});await send("Runtime.enable");const result=await send("Runtime.evaluate",{expression:`(()=>{uiSettings.appearance='light';applyUiSettings();const selectors=['body','.titlebar','.workspace>aside','.content','.settings-block','.board-topbar','.snake-panel'];return Object.fromEntries(selectors.map(selector=>{const element=document.querySelector(selector),style=getComputedStyle(element);return[selector,{bg:style.backgroundColor,color:style.color,appearance:document.body.dataset.appearance}]}));})()`,returnByValue:true});console.log(JSON.stringify(result.result.value,null,2));socket.close();}
main().catch(error=>{console.error(error);process.exit(1);});
